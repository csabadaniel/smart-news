news:
  prompt: "Give me today's top news summary."
  mail:
    from: ${GMAIL_USERNAME}
    to: ${GMAIL_USERNAME}
    subject: "Smart News"

spring:
  application:
    name: smart-news
  cloud:
    gcp:
      parametermanager:
        enabled: false
      secretmanager:
        enabled: false
  ai:
    retry:
      max-attempts: 1
    google:
      genai:
        api-key: ${GEMINI_API_KEY:dummy-key}
        chat:
          options:
            model: gemini-2.5-flash
            max-output-tokens: 2048
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USERNAME}
    password: ${GMAIL_APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
  health:
    mail:
      enabled: false

---
spring:
  config:
    activate:
      on-profile: gcp
    import: "pm@"
  cloud:
    gcp:
      parametermanager:
        enabled: true
      secretmanager:
        enabled: true
  ai:
    google:
      genai:
        chat:
          options:
            model: ${pm@projects/smart-news-20260321/locations/global/parameters/smart-news-model/versions/latest}
            max-output-tokens: 2048
  mail:
    username: ${sm://gmail-username}
    password: ${sm://gmail-app-password}
news:
  prompt: ${pm@projects/smart-news-20260321/locations/global/parameters/smart-news-prompt/versions/latest}
  mail:
    from: ${sm://gmail-username}
    to: ${sm://gmail-username}
management:
  endpoints:
    web:
      exposure:
        include: health, refresh
